# ── gRPC + protobuf (must match the client's generated stubs, protoc 6.31.1) ──
grpcio>=1.68.0
grpcio-tools>=1.68.0
protobuf>=5.28.0

# ── REST API ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
python-multipart>=0.0.12
pyjwt>=2.9.0

# ── Database ──
sqlalchemy>=2.0.36
asyncpg>=0.30.0
psycopg[binary]>=3.2.3
alembic>=1.14.0

# ── Cache / queue ──
redis>=5.2.0
celery>=5.4.0

# ── Vector DB + embeddings ──
qdrant-client>=1.12.0
fastembed>=0.4.0

# ── Object storage ──
boto3>=1.35.0

# ── LLM providers (optional at runtime; mock provider needs none) ──
anthropic>=0.39.0
openai>=1.54.0

# ── Config / utils ──
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
